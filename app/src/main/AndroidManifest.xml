<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.asus.proyectoencuestas">

    <uses-permission android:name="android.permission.INTERNET" />
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/metro"
        android:label="ENCUESTA DE VIAJES PERSONA"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"

        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCLBR6-MNfV067xhta_qre4ZOtMsA3r3KI" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SyncActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".FechaActivity" />
        <activity android:name=".PantallaEncuesta1"
            android:screenOrientation="portrait"/>
        <activity android:name=".PantallaEncuesta1_Persona"
            android:screenOrientation="portrait"/>
        <activity android:name=".PantallaEncuesta1_Viaje"
            android:screenOrientation="portrait"/>
        <activity android:name=".EncuestasActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".PantallaEncuesta6"
            android:screenOrientation="portrait"/>
        <activity android:name=".PantallaEncuesta6_Persona"
            android:screenOrientation="portrait"/>
        <activity android:name=".PantallaEncuesta6_Viaje"
            android:screenOrientation="portrait"></activity>
    </application>

</manifest>